import React from 'react';
import LessonLibrary from '../../components/student/LessonLibrary';

export default function StudentJuniorLessons({ navigation }) {
  return <LessonLibrary navigation={navigation} variant="junior" />;
}
