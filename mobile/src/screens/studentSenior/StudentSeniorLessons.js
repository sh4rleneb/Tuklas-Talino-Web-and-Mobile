import React from 'react';
import LessonLibrary from '../../components/student/LessonLibrary';

export default function StudentSeniorLessons({ navigation }) {
  return <LessonLibrary navigation={navigation} variant="senior" />;
}
